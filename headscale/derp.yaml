# DERP map served to all tailscale clients by Headscale.
# Regenerated by deploy.sh from DERP_HOSTNAME — do not edit manually in prod.
#
# Empty by default: without a DERP relay only direct (UDP hole-punched)
# connections work — fine on a LAN, broken across CGNAT or filtered networks.
regions: {}
